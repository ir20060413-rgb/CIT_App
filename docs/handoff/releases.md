# 公開手順と残っている確認

2.3.0（81）の新しい準備記録は `docs/releases/2.3.0.md`。未署名の検証用AABと全310件のテスト成功を確認。署名、Play Consoleの申告、Firebase本番との整合、実機での最終確認が完了するまで提出用と扱わない。

確認日: 2026-09-12
状態: 引き継ぎ時の確認項目。本番への反映履歴は未記入。

## 公開前に確認する

対象コミット、Flutterの版、依存ロックファイル、テスト結果、実機結果、公開担当、戻せる直前の版を記録する。ローカルビルド成功を公開済みと扱わない。

Flutterの基本確認は `flutter test` と `dart analyze lib test`。Androidは `flutter build apk --debug` で実機確認用ビルドを作り、配布用には署名設定を確認して `flutter build appbundle --release` を使う。ストアの公開権限・署名情報は管理者から引き継ぐ。

## 2026-09-12の認証・通知修正の反映順序

`docs/CODE_REVIEW_2026-09-12.md` の記録に従い、FirestoreルールとFunctionsを先に反映し、その後に対応したアプリを配布する。端末別通知登録と管理APIの変更を含むため、クライアントだけ先に配布しない。

一括認証フラグ変更用の `bulkVerifyExistingUsersNow` は、同じ公開名でHTTP 410を返す実装へ置き換える方針。単にエクスポートを消して古い関数を残さない。Firebase Authのメール認証を迂回する移行は行わない。

本番への適用時はFirebaseのプロジェクトIDと対象リソースを明示し、必要な範囲だけデプロイする。古いルールへの無条件のロールバックは権限不備を再導入する可能性があるため、直前版との差分と影響を確認する。

## 現時点で残る確認

2026-09-12の学年暦修正まで含むFlutterテスト87件とAndroidデバッグビルドは成功した。接続端末がないため最新の変更のAndroid実機表示は未確認。通知の実配信・タップ、複数アカウントと複数端末の動作も確認が必要。

iOSウィジェットのSDKビルド・署名・実機表示は未確認。`docs/HOME_WIDGETS.md` のMacでの手順を実行する。両ターゲットのApp Group、署名、ウィジェットの追加・更新・タップを確認する。

現在のFirebaseルール・Functionsの公開設定・IAM、およびこれらのローカル修正の本番反映状況は、この資料では保証しない。担当者が確認日と対象コミットを追記する。
