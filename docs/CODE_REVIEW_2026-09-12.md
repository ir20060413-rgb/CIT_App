CIT_App コードレビュー・整理結果（2026-09-12）
============================================

レビューで挙げた不具合8件の修正を実装しました。認証・更新権限、時間割の保存、ログアウト時の通知解除、通知からの移動、取得失敗の表示、ログの保護を変更し、ローカルテストで確認しています。本番への反映と実機での確認は未実施です。

**対応状況（不具合修正後に更新）**

レビューで見つかった不具合8件は実装・ローカルテストを完了しました。不要ファイルの整理と、追加依頼のナビゲーション調整も実施済みです。「修正の実装」「テスト」「実機確認」「本番反映」は分けて管理します。

| 項目 | 現在の状態 | 確認結果・残っていること |
| --- | --- | --- |
| 未使用ファイル・importの整理 | 完了 | 旧実装9ファイルと不要import4件を削除。既存テスト成功 |
| フッターの高さ | 実装・自動テスト完了 | 通常64dp。文字拡大時は高さも自動調整。各タブの操作領域を48dp以上確保 |
| 3ボタンナビゲーションへの対応 | 共通対応を実装・自動テスト完了 | 全ルートの外側で下端・左右のシステム領域を確保。詳細画面、ダイアログ、下から開くメニューも対象。二重余白、横向き、キーボード開閉、明暗テーマを検証 |
| 変更後の実機表示 | 未確認 | Samsung実機が3ボタン設定であることは確認。接続が途切れたため変更の反映と実表示の確認は未完了 |
| AndroidデバッグAPK | ビルド成功 | OneDrive内の生成ファイルがロックされるため、同じソースを一時フォルダーにコピーして検証。実機へのインストールは未実施 |
| セットアップ用バックアップ2フォルダー | 未削除 | 自動承認レビューで削除を拒否。Gitと解析対象からは除外済み |

Flutter全62件、Firestore Emulatorの権限テスト14件、Functions全26件が成功しています。リリース時の定数を有効にしたログテスト4件も成功しました。Dartの静的解析はエラー0、警告46、情報430です。残っている警告を含め、プロジェクト全体の解析が完全にクリーンになったわけではありません。

生成した[デバッグAPK](C:/Users/mmura/AppData/Local/Temp/cit-review-1789153936424/build/app/outputs/flutter-apk/app-debug.apk)は一時フォルダー内にあります。最終ビルド後に `lib` 配下388ファイルと依存関係のロックファイルが作業フォルダーと一致することを確認しました。実機で使う前に、以下の順序でバックエンドを反映する必要があります。

**修正した8件と検証範囲**

| 順番 | 対象（詳細の指摘番号） | 実装したこと | ローカルで確認したこと |
| --- | --- | --- | --- |
| 1 | 投稿の更新権限（1） | 投稿者の編集、管理者の承認、閲覧数・クーポン利用を個別に制限 | 他人の本文・投稿者ID・承認状態の改変、閲覧数に便乗したフィールド追加・削除を拒否。正規の編集・承認・クーポン更新を許可 |
| 2 | メール認証（2） | Firebase Authの検証結果と検証済みトークンを使用。プロフィールの認証項目も保護 | 未認証プロフィールの自己申告を拒否。Authの認証済み・未認証状態を画面判定へ反映。同じ利用者のトークン更新で判定が揺れない |
| 3 | 管理用API（3） | 管理者のIDトークン検証を共通化。更新はPOST、統計取得はGET。一括認証フラグ変更は処理を廃止しHTTP 410を返す | 未ログイン・失効トークン・一般利用者・未認証管理者・不正メソッドを拒否。認可に失敗したとき管理処理を実行しない |
| 4 | 時間割の保存（4） | 最新データを検証し、変更を1回のトランザクションで確定 | 重複・範囲外・書き込み失敗・古い編集画面からの保存／削除で原本を維持。連続コマの増減と削除も確認 |
| 5 | ログアウト後の通知（5） | 端末別登録へ移行。登録解除→予約通知・ウィジェット・キャッシュ消去→ログアウトの順で処理 | A→B切替時にこの端末だけ解除し、別端末を保持。解除失敗時はログアウト成功にしない。遅れて完了する旧セッションの処理を無効化 |
| 6 | 通信失敗の表示（7） | 取得エラーを上位へ伝え、前回表示と再試行を提供 | 未登録と取得失敗を区別。初回失敗と更新失敗を別表示し、更新失敗時は前回の時間割を維持 |
| 7 | 通知からの移動（6） | 起動待ちの通知を保持し、掲示板投稿・コメント、Cwitter投稿・プロフィール、ちばちゃんねるへ遷移。対象を開けない場合は通知一覧へ案内 | 宛先の照合、起動待ちキューの一度だけの消費、ログアウト時の破棄、遷移情報の往復をテスト。OS経由の実際の配信・タップは実機確認が必要 |
| 8 | ログの情報保護（8） | 認証・通知関連の診断出力を共通ロガーへ移行。トークン・メール等をマスクし、通常診断をデバッグ限定に変更 | ダミーメール・FCMトークン・Bearer認証・URLクエリをマスク。リリース定数が有効であることと、診断出力が消えることをテスト |

**本番反映時の順序と残る確認**

1. FirestoreルールとFunctionsを先に反映する。対象の管理APIに加え、個別・全体プッシュの送信処理も端末別登録に対応している。旧形式の単一トークンも読み取れる。
2. `bulkVerifyExistingUsersNow` は同じ公開名のまま、データを変更しないHTTP 410の実装へ置き換える。エクスポートを単に消して古い関数を残さない。
3. 更新版アプリを配布する。端末別登録への初回移行時にFCMトークンを更新し、旧版が残した関連付けを失効させる。他端末の登録は削除しない。
4. 未認証／認証済みの実アカウント、管理者API、2アカウント・2端末の通知、起動中／背景／終了状態の通知タップ、3ボタン設定での表示を確認する。

Firestore上の認証フラグだけがtrueだった利用者は、Firebase Authでメール認証を完了する必要がある。全員を認証済みとみなす移行処理は用意していない。本番ルール・Functionsの公開設定・IAMは未確認で、本番データへの書き込みやデプロイは行っていない。

再現用コマンドは `flutter test`、`npm test --prefix functions`、`npm run test:rules`。ルールテストは `firebase.test.json` と `demo-cit-review` を使い、実プロジェクトへ接続しない。EmulatorにはJava 21を使用した。ログのリリース検証は `flutter test test/utils/secure_logger_test.dart --dart-define=dart.vm.product=true --dart-define=EXPECTED_LOG_MODE=release`。

**UX改善・新機能との区別**

- 不具合修正：上記8件。既存機能が安全に期待どおり動くための作業。
- UX改善：次の授業をホームで見つけやすくする、下書きを保持する、文字拡大時の各画面を整える。ナビゲーションの文字拡大対応だけで、全画面のアクセシビリティ確認が完了した扱いにはしない。
- 新機能：最初は「次の授業→教室マップ」。その後に課題・締切管理、学期切替アシスタント、学内横断検索を検討する。いずれも提案段階で未実装。

以下は修正前のレビュー記録です。行番号と問題の説明はレビュー時点のもので、現在の対応状況は上の表を参照してください。

レビュー時点の作業ツリーを対象に、起動・認証・Firestoreルール・管理用Functions・時間割・通知・ホーム・Dartの参照関係を確認しました。既存の未コミット変更が多数あるため、既存変更を保持したまま、後続の修正を実装しています。指摘の確認に本番データを書き換える操作は行っていません。

**優先度P1：早めに修正する問題**

1. **掲示板の他人の投稿を更新できるルールになっている。**

   [firestore.rules:147](C:/Users/mmura/OneDrive/Desktop/CIT_App/firestore.rules:147) では、ログインしているだけで `bulletin_posts` の任意の更新を許可しています。投稿者・管理者・大学ドメインの確認も、変更可能なフィールドの制限もありません。他人の本文、投稿者情報、承認状態などの書き換えがルール上可能です。「画面から操作できない」ことでは保護できません。

   推奨：投稿者による本文編集、管理者による承認、一般利用者によるリアクションを別条件に分け、投稿者IDの変更を禁止する。変更可能なフィールドは `affectedKeys().hasOnly(...)` で明示する。Firestoreの公式資料でも、変更フィールドの制限に `affectedKeys()` を使っています。[Firebase公式：フィールド単位のアクセス制御](https://firebase.google.com/docs/firestore/security/rules-fields)

   確認ケース：未ログイン、他人、投稿者、管理者それぞれで本文・承認・投稿者IDの更新を検証する。

2. **メール認証の判定が、本人が変更できるプロフィールの値を信用している。**

   [simple_auth_provider.dart:34](C:/Users/mmura/OneDrive/Desktop/CIT_App/lib/core/providers/simple_auth_provider.dart:34) はFirestoreの `emailVerified` を画面アクセス判定に使用しています。一方、[firestore.rules:326](C:/Users/mmura/OneDrive/Desktop/CIT_App/firestore.rules:326) は本人によるその値の作成・変更を制限していません。さらに [firestore.rules:20](C:/Users/mmura/OneDrive/Desktop/CIT_App/firestore.rules:20) の大学利用者判定には、メールの所有確認を示す `email_verified` の条件がありません。

   この組み合わせでは、許可ドメインの未認証アカウントでもプロフィールの値を変更して画面側の認証待ちを回避でき、大学メールの所有確認がアクセス制御になっていません。

   推奨：認証済みかどうかはFirebase Authの検証済み情報を正として扱い、プロフィール表示情報と権限情報を分離する。認証前のプロフィール作成に必要なアクセスだけ個別に許可し、保護対象のコレクションでは検証済みトークンを要求する。[Firebase公式：認証情報を使ったルール](https://firebase.google.com/docs/rules/basics)

   確認ケース：未認証ユーザーの自己申告ではアクセスできず、正規の認証後・トークン更新後にアクセスできること。旧メールドメインの既存ユーザー移行も別途検証する。

3. **全ユーザーの認証フラグを更新するHTTP処理に認証・管理者確認がない。**

   [functions/index.js:1906](C:/Users/mmura/OneDrive/Desktop/CIT_App/functions/index.js:1906) の `bulkVerifyExistingUsersNow` は、リクエストの送信者を確認せず全ユーザーのFirestore上の認証フラグをtrueに更新します。OPTIONS以外のHTTPメソッドも制限していません。公開状態でデプロイされていれば、管理者以外からもこの処理を起動できる実装です。Firebase Auth側のメール検証を実施する処理ではありません。

   同じく認証確認がない管理用途の処理は、メニュー更新（876行）、サークル同期（1522行）、ユーザー統計取得（1814行）にもあります。

   推奨：一度限りの救済処理は一般公開のHTTP関数から外し、必要なら管理者認証、対象ユーザーの限定、実行前の件数確認を備えた管理用処理にする。手動更新APIはPOSTに限定し、認証・権限チェックを共通化する。実際の公開状態はIAMで確認する必要があります。[Google Cloud公式：関数呼び出しの認証](https://docs.cloud.google.com/functions/docs/securing/authenticating)

   確認ケース：未認証・一般ユーザー・GETを拒否し、許可した管理者と対象だけを処理する。テストではAdmin SDKを差し替え、本番エンドポイントを呼び出さない。

4. **時間割編集が失敗したとき、元の授業が消える。**

   [schedule_edit_screen.dart:519](C:/Users/mmura/OneDrive/Desktop/CIT_App/lib/screens/schedule/schedule_edit_screen.dart:519) は、編集対象の講義を先に削除し、その後に連続時限の範囲・重複を確認します。例えば1限の講義を2コマ連続へ変更し、2限に別講義がある場合、重複エラーになる前に元の1限は削除済みです。保存も時限ごとに別の書き込みなので、途中の通信失敗で一部だけ残る可能性があります。

   推奨：最新データに対して全時限を検証し、旧講義の除去と新講義の配置を1回のFirestoreトランザクションで確定する。画面とProviderに重複している追加ロジックはサービスにまとめる。

   確認ケース：重複、10限超過、連続コマ数の増減、通信失敗、他端末の同時編集で、失敗時に元データが保持されること。

5. **ログアウト後も前のアカウント宛ての通知が同じ端末へ届き得る。**

   [simple_profile_screen.dart:1211](C:/Users/mmura/OneDrive/Desktop/CIT_App/lib/screens/profile/simple_profile_screen.dart:1211) と [auth_provider.dart:230](C:/Users/mmura/OneDrive/Desktop/CIT_App/lib/core/providers/auth_provider.dart:230) はAuthのサインアウトだけを実施します。[notification_service.dart:82](C:/Users/mmura/OneDrive/Desktop/CIT_App/lib/services/notification/notification_service.dart:82) のユーザーとFCMトークンの登録を解除していません。サーバーは [functions/index.js:1747](C:/Users/mmura/OneDrive/Desktop/CIT_App/functions/index.js:1747) でその登録を使って送信します。

   アカウントAでログアウトした後もAの登録が残り、同端末でBにログインすると同じ端末トークンが両者に紐づき得ます。予約済みの授業通知や端末ウィジェットも、ログアウト処理で明示的に消していません。

   推奨：共通のログアウト処理に統一し、認証が有効なうちに「この端末」の通知登録を解除する。ローカル通知・ウィジェット・ユーザー別キャッシュも初期化する。複数端末利用を考慮して、ユーザーごとに単一トークンを持つ構成から端末別登録へ移行する。

   確認ケース：A→ログアウト→Bの切り替えでA宛て通知が届かないこと。他端末のAの登録は維持されること。通信不能時の解除失敗も扱う。

**優先度P2：使い勝手と運用の改善**

6. **投稿通知をタップしても対象の投稿に移動しない。**

   [notification_service.dart:124](C:/Users/mmura/OneDrive/Desktop/CIT_App/lib/services/notification/notification_service.dart:124) の通知タップ処理はTODOのままです。フォアグラウンド受信時のローカル通知にも遷移用データを渡していません。通知そのものが届いても、利用者は対象を再検索する必要があります。

   推奨：通知種別・投稿ID・コメントIDを共通の遷移データにし、ログインと画面準備の完了後に処理する。削除済み・閲覧権限なしの場合は通知一覧へ戻せる表示を用意する。授業通知にはすでに起動時データを保留する仕組みがあり、設計の参考にできる。

   確認ケース：起動中・バックグラウンド・完全終了・未ログインからの通知タップをそれぞれ確認する。

7. **時間割の取得失敗を「データがない」として扱っている。**

   [schedule_service.dart:64](C:/Users/mmura/OneDrive/Desktop/CIT_App/lib/services/schedule/schedule_service.dart:64) は取得例外をnullへ、同ファイルの `getAllSchedulesByUserId` は取得例外を空リストへ変換します。Providerには成功として届くため、UIが「未登録」と「通信・権限エラー」を区別できません。保存済みデータが消えたように見える原因になります。

   推奨：存在しない場合だけnull/空リストを返し、取得失敗はエラーとしてUIまで渡す。直前の表示を保持したまま「更新できませんでした」と再試行を表示する。

   確認ケース：未登録、通信エラー、権限エラー、保存済みキャッシュありの状態を別々に検証する。

8. **通知トークンや通知本文を通常ログへ出している。**

   [notification_service.dart:95](C:/Users/mmura/OneDrive/Desktop/CIT_App/lib/services/notification/notification_service.dart:95) はFCMトークンをそのまま `print` します。同ファイルのトークン更新処理、`main.dart` のユーザーメールや通知本文の出力にも、デバッグビルド限定のガードがありません。既存の `SecureLogger` はアプリ本体から参照されておらず、これらの出力を保護していません。

   推奨：生のトークン・メール・通知本文は出力せず、イベント種別と成功/失敗だけを記録する。必要な診断ログは共通ロガー経由にし、例外文字列とURLのクエリも確認する。

   確認ケース：ダミートークンとダミーメールを使い、リリースログに値が現れないことを確認する。

**今回削除したファイル**

`main.dart` とテストからのimport/export/partの到達性を調査し、ファイル名・公開クラス名の参照も検索しました。削除した9ファイルはすべてアプリとテストから未参照で、現行実装が別に存在する旧実装です。削除量は約3,200行、約106KBです。

| 削除したファイル（リポジトリ内） | 判断理由・現行実装 |
| --- | --- |
| `lib/widgets/image_viewer/fullscreen_image_viewer.dart` | 未使用の旧画像ビューアー。現行は `widgets/common/interactive_fullscreen_image_viewer.dart` 等 |
| `lib/widgets/image_viewer/image_viewer/fullscreen_image_viewer.dart` | さらに重複していた旧画像ビューアー |
| `lib/models/schedule_model.dart` | 旧データ構造。現行は `models/schedule/schedule_model.dart` |
| `lib/services/cache_service.dart` | 未使用の旧キャッシュ。現行は `core/services/cache_service.dart` |
| `lib/core/services/offline_service.dart` | 未使用の旧オフライン処理。起動処理は `SimpleOfflineService` を利用 |
| `lib/widgets/performance/offline_aware_widget.dart` | 呼び出し元のない旧オフライン表示群 |
| `lib/widgets/schedule/academic_year_header_button.dart` | 呼び出し元のない旧年度切替ボタン |
| `lib/widgets/schedule/academic_year_selector.dart` | 呼び出し元のない旧年度選択UI |
| `lib/widgets/schedule/schedule_class_card.dart` | 呼び出し元のない旧授業カード |

ホーム画面の静的解析で未使用と判定されたimportも4件削除しました。管理・診断用途のコード、元画像、運用スクリプトは、単にアプリから参照されないことだけでは不要と断定できないため残しています。一時非表示の電車機能もテストと既存の意図があるため残しています。

セットアップ時の次のバックアップ2フォルダーは、4,409ファイル・合計1,880,233,687バイト（約1.88GB）あります。削除は自動承認レビューで拒否されたため、**未削除**です。通常の `build` と `.dart_tool` は現役の生成物として維持しています。残ったバックアップが解析対象やGitへの追加対象に混ざらないよう、`.gitignore` と解析設定にこの名前のパターンを追加しました。

- `build.setup-backup-20260912-025531`
- `.dart_tool.setup-backup-20260912-025531`

**UX改善の優先順位**

| 順番 | 改善 | 実装上の根拠と最初の対応 | 完了の目安 |
| --- | --- | --- | --- |
| 1 | 保存・通信失敗で利用者を迷わせない | 指摘4・7。失敗時の原本保持、再試行、最終更新時刻、前回表示の保持 | 通信エラーでも入力・保存済み時間割が消えたように見えない |
| 2 | 通知から目的の場所へ直接移動 | 指摘6。投稿・返信への共通遷移と削除済みの案内 | 通知から目的の内容まで1タップ |
| 3 | ホームを「次の行動」中心にする | 現在の初期カード順は天気→時間割→学食→バス。授業が近いときは授業名・時刻・教室を上に出す。既存の並び替え設定も尊重する | 次の授業と教室がスクロールなしで分かる |
| 4 | 投稿・問い合わせ・時間割編集の下書きを保護 | 入力欄、離脱確認、送信状態が各画面に分散。ユーザー別の下書きと明示的な再送を共通化する | 戻る操作や通信切断で入力が失われない |
| 5 | 大きい文字・狭い画面でも主要操作を保つ | ホーム約5,889行、地図約5,138行にUIが集中。まず主要カードを単独で確認できる構成に分離する | 文字拡大200%、画面幅320dp、読み上げで保存・通知・地図検索を操作できる |

全面的なフォルダー再編より、変更頻度の高い「次の授業」「学食」「バス」からサービスとWidgetを分離する進め方を推奨します。ホームの1分タイマーによるデータ再取得も、時刻だけの更新と通信が必要な更新に分けて計測します。

**新機能の提案**

| 優先 | 機能 | 最小構成 | 既存機能とのつながり・注意点 |
| --- | --- | --- | --- |
| A | 次の授業から教室までの案内 | 授業カードの教室名から教室検索・該当階の地図を開く | 時間割と教室マップをつなぐ。最初は対応済み教室のみ扱い、未対応は検索結果へ |
| A | 課題・提出期限リスト | 科目に紐づく課題名、締切、完了状態、ホームの直近締切3件 | 時間割のメモを読むだけでは難しい締切管理を補う。MVPは手入力、学内サイト連携は後回し |
| B | 学期切替アシスタント | 前学期の時間割を複製し、残す科目を確認、Excel取り込みとの差分を表示 | 既存の複数時間割・Excel取り込みを活用。元の時間割を上書きしない |
| B | 学内横断検索 | 教室・便利リンク・サークルを同じ検索欄から探す | 構造化済みの対象から始める。掲示板を加える際は閲覧権限・ブロック・削除状態を反映する |

最初に着手するなら、権限と保存処理の修正後に「次の授業→教室マップ」を推奨します。既存機能を使って利用者の操作を減らせ、機能追加の範囲も限定できます。

**レビュー・整理時点の検証結果と限界**

| 項目 | 結果 |
| --- | --- |
| Flutter既存テスト | 23件成功 |
| Functions既存テスト | 10件成功 |
| Dart静的解析（lib、整理前） | エラー0、警告55、情報967 |
| Dart静的解析（lib、整理後） | エラー0、警告47、情報945 |
| 除外設定後のプロジェクト全体解析 | エラー0、警告47、情報945。バックアップ由来の診断0 |
| 差分の空白エラー確認 | 問題なし |

上の数値は修正前の記録です。その後、Firestore Emulatorの権限テスト、時間割の失敗時保持、アカウント切替時の通知登録、認証、エラー表示、ログのテストを追加しました。最新結果は冒頭に記載しています。網羅的なセキュリティ監査や全画面の実機UXテストを完了したものではありません。
